// script.js
document.addEventListener('DOMContentLoaded', function () {
    // getElementById を使用して要素にアクセス
    var contentElement = document.getElementById('content');

    // 要素の内容を変更
    contentElement.innerHTML += '<p>GitHub makes web development fun!</p>';

    // その他の操作を追加
    // ...
});
